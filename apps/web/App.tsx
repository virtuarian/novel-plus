
// import { useState } from 'react';
// import LoginPopup from '../../packages/headless/LoginPopup';
// import { isLoggedIn } from '../../packages/headless/sessionManager';

// const App: React.FC = () => {
//     const [showLogin, setShowLogin] = useState(false);

//     // Check if the user is logged in
//     if (!isLoggedIn()) {
//         setShowLogin(true);
//     }

//     return (
//         <div>
//             <h1>Welcome to the App</h1>
//             {showLogin && <LoginPopup onClose={() => setShowLogin(false)} />}
//         </div>
//     );
// };

// export default App;
